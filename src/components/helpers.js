import { Dimensions } from "react-native";
export const deviceWidth = Math.round(Dimensions.get('window').width);
export const deviceHeight = Math.round(Dimensions.get('window').height);
export const HEADERFONTSIZE = 10